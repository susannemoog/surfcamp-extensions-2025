{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Database",
            "type": "shell",
            "command": "mysql -u db -h db -pdb db",
            "presentation": {
                "group": "buildGroup",
                "reveal": "never",
                "panel": "new",
                "echo": false,
            },
            "runOptions": {
                "runOn": "folderOpen" // This starts both tasks when the container is started
            }
        }
    ]
}